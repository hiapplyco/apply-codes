/**
 * Backward-compatible barrel export.
 * Types have been split into src/types/google/ for better organization.
 * New code should import from '@/types/google' directly.
 */
export * from './google';
