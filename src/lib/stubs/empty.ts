// Empty stub for Node.js built-in modules that googleapis imports.
// These are never called at runtime in the browser — the actual Google API
// calls go through Firebase Cloud Functions, not the client.
export default {};
